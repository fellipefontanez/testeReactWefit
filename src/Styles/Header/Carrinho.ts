import { styled } from "styled-components";

const StyledCarrinhoHeader = styled.div`
  display: flex;
  align-items: center;
  gap: 8px;
  color: #999999;
  font-size: 12px;
  font-weight: 600;
  cursor: pointer;
  span {
    display: flex;
    text-align: center;
    vertical-align: baseline;
  }
`;

export default StyledCarrinhoHeader;
